external-url-viewer "urlview"

# feed options
auto-reload yes
reload-time 60
reload-threads 8
download-retries 2
download-timeout 10
download-full-page yes
scrolloff 11
always-display-description true
text-width 120
goto-next-feed no

# keybindings
bind-key j down
bind-key k up
bind-key j next articlelist
bind-key k prev articlelist
bind-key J next-feed articlelist
bind-key K prev-feed articlelist
bind-key G end
bind-key g home
bind-key d pagedown
bind-key u pageup
bind-key l open
bind-key h quit
bind-key a toggle-article-read
bind-key n next-unread
bind-key N prev-unread
bind-key D pb-download
bind-key U show-urls
bind-key x pb-delete
bind-key ENTER open-in-browser

browser "lynx %u"
#browser "(firefox %u >/dev/null 2>&1) &"
macro o set browser "mpv %u &> /dev/null &" ; open-in-browser ; set browser "elinks %u"

# colors
color listfocus white red
color listfocus_unread white red bold
color title red black
color info yellow black
color hint-key yellow black

feedlist-format "%?T?║%n %12u %t &╠ %t?"

macro i set pager "~/Development/bash-scripts/kitty-img-pager/kitty-img-pager.sh"; open; set pager internal
